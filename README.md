A slighly faster version of the python scrypt module. It requires openssl for compiling.

Setup instructions (Ubuntu)
    sudo apt-get install libssl-dev
    cd vertcoin_scrypt
    sudo python setup.py install

